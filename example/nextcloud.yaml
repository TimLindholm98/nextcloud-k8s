nextcloud:
  nextcloudDomain: &nextcloudDomain change.me
  # Nextcloud configuration
  nextcloud:
    trustedDomains:
      - *nextcloudDomain
      - localhost
    ingress:
      enabled: true
    storage:
      size: 10Gi